# SPEC.md

# Windows 图片中间裁切工具

## 1. 项目目标

开发一个面向 Windows 的轻量图片工具，核心用途是：

> 打开一张已有图片，选择“中间裁切”工具，在图片中拖出一段横向或纵向区域，删除该区域，并让剩余两部分自动无缝合拢。

交互目标参考 Picsew 的“简单、直接、少步骤”体验，但本项目只实现当前明确需要的能力，不做完整图片编辑器。

可参考 ShareX 的 `CutOut` 实现思路，避免重复造轮子；但不要直接复制 ShareX 大段源码或整个编辑器架构。

---

## 2. 非目标

第一版明确不做以下功能：

- 不做截图工具。
- 不做长截图。
- 不做图片拼接。
- 不做图层系统。
- 不做滤镜。
- 不做文字、画笔、箭头、马赛克等标注。
- 不做 AI 功能。
- 不做批量处理。
- 不做云同步。
- 不做账号系统。
- 不做复杂插件系统。
- 不做多文档标签页。
- 不做 macOS / Linux 适配。
- 不追求 Photoshop / PhotoDemon / ShareX 级别的完整编辑能力。

第一版只围绕“打开图片 → 中间裁切 → 撤销/重做 → 保存”做好体验。

---

## 3. 平台与技术栈

### 3.1 目标平台

- Windows 10
- Windows 11
- x64 优先

### 3.2 推荐技术栈

- C#
- .NET 10
- Avalonia
- SkiaSharp

原因：

- ShareX 当前图片编辑器已经使用 Avalonia + SkiaSharp，可直接参考成熟的数据流和图像处理方式。
- SkiaSharp 足以完成本项目需要的裁切、绘制和保存。
- Avalonia 在 Windows 上开发简单，且方便后续扩展。
- 不需要引入更重的图像处理框架。

### 3.3 项目结构建议

保持简单：

```text
src/
  App/
    App.axaml
    App.axaml.cs
    MainWindow.axaml
    MainWindow.axaml.cs

  Core/
    ImageDocument.cs
    CutOutOperation.cs
    HistoryManager.cs
    ImageFileService.cs

  Controls/
    ImageCanvas.cs

tests/
  Core.Tests/
```

不要为了“架构完整”拆成过多层。

---

# 4. 核心用户流程

## 4.1 启动状态

应用启动后：

- 显示空白主界面。
- 中央显示提示：
  - “拖入图片”
  - “或点击打开”
- 此时不处于中间裁切状态。
- 默认是普通查看状态。

重要：

> 不要把“中间裁切”设为应用启动后的默认激活工具。

用户必须先打开图片，再主动点击“中间裁切”。

---

## 4.2 打开图片

支持以下方式：

### 方式 A：点击打开

点击顶部“打开”按钮：

- 弹出系统文件选择器。
- 选择图片后加载。

### 方式 B：拖拽打开

用户从资源管理器把图片拖进窗口：

- 直接加载图片。

### 支持格式

第一版：

- PNG
- JPG
- JPEG
- WebP
- BMP

如果格式无法读取：

- 显示明确错误提示。
- 不崩溃。
- 不改变当前已打开图片。

---

# 5. 主界面

## 5.1 界面原则

目标：

- 简洁
- 少按钮
- 不模仿传统复杂图片编辑器
- 用户第一次打开也能理解

主界面只保留必要控件。

---

## 5.2 顶部工具栏

从左到右：

```text
打开 | 中间裁切 | 撤销 | 重做 | 保存 | 另存为
```

可选：

```text
实际大小 | 适合窗口
```

第一版不要增加更多工具。

---

## 5.3 主画布

中央区域显示图片。

要求：

- 自动居中。
- 图片过大时自动缩小以适合窗口。
- 图片过小时默认不强制放大超过 100%，除非用户主动缩放。
- 背景使用中性灰色或系统默认编辑区背景。
- 图片边界清晰可见。

---

# 6. 中间裁切工具

这是项目核心功能。

---

## 6.1 进入工具

用户点击：

```text
中间裁切
```

后：

- 按钮进入激活状态。
- 鼠标进入裁切交互。
- 图片本身不立即变化。
- 显示简短提示：

```text
拖动选择要删除的横向或纵向区域
```

按 `Esc`：

- 退出当前未完成选区。
- 保留中间裁切工具激活状态。

再次点击“中间裁切”：

- 退出中间裁切工具。
- 回到普通查看状态。

---

# 7. 选区交互

## 7.1 基本行为

用户在图片上：

1. 按下鼠标左键。
2. 拖动。
3. 松开。

程序根据拖动方向判断：

- 横向裁切
- 纵向裁切

---

## 7.2 横向裁切

如果用户的主要拖动方向是上下：

程序理解为：

> 删除图片中一整条横向区域。

例如用户从：

```text
Y = 820
```

拖到：

```text
Y = 1120
```

程序最终删除：

```text
Y = 820 ~ 1120
```

删除区域自动扩展为整张图片宽度：

```text
X = 0 ~ imageWidth
```

用户不需要手动从图片左边拖到最右边。

---

## 7.3 纵向裁切

如果用户主要拖动方向是左右：

程序理解为：

> 删除图片中一整条纵向区域。

例如：

```text
X = 900 ~ 1250
```

删除区域自动扩展为整张图片高度：

```text
Y = 0 ~ imageHeight
```

---

# 8. 横向 / 纵向方向判断

使用简单规则即可。

记录：

```text
deltaX = abs(currentX - startX)
deltaY = abs(currentY - startY)
```

判断：

```text
deltaY > deltaX
=> 横向裁切

deltaX >= deltaY
=> 纵向裁切
```

不需要复杂手势识别。

如果后续实际体验发现斜拖容易误判，可再加阈值。

第一版不要提前过度设计。

---

# 9. 选区预览

用户拖动时，不要立即修改图片。

显示半透明遮罩。

例如横向：

```text
┌──────────────────────────┐
│                          │
│        保留区域           │
│                          │
├──────────────────────────┤
│████████ 删除区域 ████████│
├──────────────────────────┤
│                          │
│        保留区域           │
│                          │
└──────────────────────────┘
```

遮罩要求：

- 覆盖将被删除的完整横条或竖条。
- 选区边缘清晰。
- 拖动时实时更新。
- 不修改原图。

---

# 10. 尺寸提示

拖动过程中，在选区附近显示：

横向：

```text
删除 300 px
结果：3840 × 1860
```

纵向：

```text
删除 420 px
结果：3420 × 2160
```

这个提示只用于帮助用户确认范围。

不要显示过多数据。

---

# 11. 确认裁切

## 11.1 第一版交互

鼠标松开后：

- 保留选区预览。
- 不立即执行破坏性裁切。

显示一个小型浮动确认条：

```text
确认删除 | 取消
```

同时支持：

```text
Enter = 确认
Esc = 取消
```

这样比“松开鼠标立即裁切”更安全。

---

## 11.2 确认后

执行：

- 删除选中区域。
- 自动把两侧内容合拢。
- 更新图片尺寸。
- 清除选区。
- 保持“中间裁切”工具激活。

保持工具激活的原因：

用户经常需要连续删除多段区域。

例如：

```text
删除第 1 段
↓
继续拖动
↓
删除第 2 段
↓
继续拖动
↓
删除第 3 段
```

不需要每次重新点击工具。

---

# 12. 核心裁切算法

不要做图像缩放。

不要重新采样。

不要压缩。

只做像素区域复制。

---

## 12.1 横向裁切

原图：

```text
width = W
height = H
```

删除：

```text
startY
endY
```

先规范：

```text
top = min(startY, endY)
bottom = max(startY, endY)
```

删除高度：

```text
cutHeight = bottom - top
```

新高度：

```text
newHeight = H - cutHeight
```

创建：

```text
newBitmap(W, newHeight)
```

复制：

### 上半部分

源：

```text
x = 0
y = 0
width = W
height = top
```

目标：

```text
x = 0
y = 0
```

### 下半部分

源：

```text
x = 0
y = bottom
width = W
height = H - bottom
```

目标：

```text
x = 0
y = top
```

结果：

```text
上半部分
+
下半部分
```

中间区域完全消失。

---

## 12.2 纵向裁切

原图：

```text
width = W
height = H
```

删除：

```text
startX
endX
```

规范：

```text
left = min(startX, endX)
right = max(startX, endX)
```

删除宽度：

```text
cutWidth = right - left
```

新宽度：

```text
newWidth = W - cutWidth
```

创建：

```text
newBitmap(newWidth, H)
```

复制：

### 左边

源：

```text
x = 0
y = 0
width = left
height = H
```

目标：

```text
x = 0
y = 0
```

### 右边

源：

```text
x = right
y = 0
width = W - right
height = H
```

目标：

```text
x = left
y = 0
```

---

# 13. 参考 ShareX

允许研究：

```text
Repository:
ShareX/ShareX
```

重点参考：

```text
ShareX.ImageEditor/
  Core/
    Annotations/
      Shapes/
        CutOutAnnotation.cs

    ImageEffects/
      Helpers/
        ImageHelpers.cs

    Editor/
      EditorCore.cs

    History/
      EditorHistory.cs
      EditorMemento.cs
```

重点理解：

- CutOut 的横向 / 纵向定义。
- 两块像素区域如何重新合并。
- 图片编辑前如何记录历史状态。
- 编辑后的图片尺寸如何更新。
- 鼠标拖动时如何区分横向和纵向。

不要：

- 整体复制 ShareX 图片编辑器。
- 引入 ShareX 大量无关依赖。
- 直接复制大段 GPL 源码到本项目。
- 为了“像 ShareX”而加入截图、标注、上传等功能。

实现应根据本 SPEC 独立完成。

---

# 14. GPL 注意事项

ShareX 使用 GPL 许可证。

本项目允许：

- 阅读其实现。
- 研究算法。
- 研究交互方式。
- 研究模块划分。

本项目默认要求：

> 独立实现，不复制 ShareX 大段源码。

如果 Codex 决定直接复用任何 ShareX 源码，必须先停止并明确报告：

```text
准备复用的文件：
具体代码范围：
许可证影响：
```

未经明确决定，不要直接复制。

---

# 15. 坐标系统

这是实现中最重要的细节之一。

必须严格区分：

```text
屏幕坐标
```

和：

```text
原始图片像素坐标
```

---

## 15.1 示例

原图：

```text
3840 × 2160
```

窗口显示：

```text
960 × 540
```

缩放比例：

```text
scale = 0.25
```

用户在屏幕拖动：

```text
Y = 200 ~ 300
```

对应原图：

```text
Y = 800 ~ 1200
```

必须转换后再执行 CutOut。

禁止直接拿 UI 坐标修改原图。

---

## 15.2 转换

假设：

```text
displayScale
imageOffsetX
imageOffsetY
```

则：

```text
imageX = (mouseX - imageOffsetX) / displayScale
imageY = (mouseY - imageOffsetY) / displayScale
```

最终：

- Clamp 到图片范围。
- 转为整数像素。
- 再执行裁切。

---

# 16. Zoom

第一版至少支持：

```text
鼠标滚轮 + Ctrl
```

进行缩放。

也支持：

```text
适合窗口
实际大小
```

范围建议：

```text
10% ~ 800%
```

默认：

- 打开大图：适合窗口。
- 打开小图：最大 100%。

Zoom 只影响显示。

绝对不能改变原图像素尺寸。

---

# 17. 平移

当图片放大超过窗口：

- 鼠标中键拖动平移。

或者：

- 按住 Space + 左键拖动。

二选一即可。

推荐：

```text
Space + 左键
```

因为更符合图片编辑器习惯。

---

# 18. Undo / Redo

必须支持：

```text
Ctrl + Z = 撤销
Ctrl + Y = 重做
```

---

## 18.1 第一版策略

不需要复杂 Command Pattern。

可以使用简单历史栈：

```text
undoStack
redoStack
```

每次真正修改图片前：

```text
当前图片状态 -> undoStack
```

修改成功后：

```text
redoStack.Clear()
```

撤销：

```text
current -> redoStack
undoStack.Pop() -> current
```

重做：

```text
current -> undoStack
redoStack.Pop() -> current
```

---

## 18.2 历史数量

默认最多：

```text
20
```

次。

避免超大图片连续编辑造成内存无限增长。

如果超过 20：

- 删除最老历史。

第一版不做复杂磁盘缓存历史。

---

# 19. 文件保存

支持：

```text
Ctrl + S
```

和：

```text
Ctrl + Shift + S
```

---

## 19.1 Ctrl + S

如果图片来自已有文件：

默认：

- 保存回原路径。

但保存前，如果当前内容发生过修改：

第一次覆盖时弹出确认：

```text
覆盖原文件？
```

按钮：

```text
覆盖
另存为
取消
```

为了避免误操作。

本次会话中同一文件确认一次即可。

---

## 19.2 Ctrl + Shift + S

始终弹出另存为窗口。

---

# 20. 输出格式

### PNG

- 保留透明通道。
- 无损保存。

### JPG / JPEG

- 默认质量：95。
- 不需要暴露高级编码参数。

### WebP

如果 SkiaSharp 当前环境稳定支持编码：

- 保留。

如果保存 WebP 存在明显兼容问题：

- 第一版允许仅打开 WebP。
- 另存时提供 PNG / JPG。

不要为了 WebP 加复杂兼容层。

---

# 21. 修改状态

窗口标题显示：

未修改：

```text
example.png
```

已修改：

```text
* example.png
```

---

# 22. 关闭未保存文件

如果存在未保存修改：

用户关闭窗口时弹出：

```text
是否保存修改？
```

按钮：

```text
保存
不保存
取消
```

---

# 23. 打开另一张图片

如果当前图片存在未保存修改：

拖入另一张图片或点击打开时：

必须先提示：

```text
当前图片尚未保存。
```

按钮：

```text
保存
不保存并打开
取消
```

---

# 24. 快捷键

第一版固定：

```text
Ctrl + O
打开

Ctrl + S
保存

Ctrl + Shift + S
另存为

Ctrl + Z
撤销

Ctrl + Y
重做

C
进入 / 退出中间裁切工具

Enter
确认当前裁切

Esc
取消当前裁切选区

Ctrl + 0
适合窗口

Ctrl + 1
100% 实际大小

Space + 左键
平移

Ctrl + 鼠标滚轮
缩放
```

如果某快捷键和 Avalonia 默认行为冲突，以不影响核心功能为准。

---

# 25. 状态栏

底部状态栏保持简单。

左侧：

```text
3840 × 2160
```

中间可显示：

```text
100%
```

中间裁切拖动时：

```text
删除 300 px → 3840 × 1860
```

不要增加文件体积、DPI、EXIF 等无关信息。

---

# 26. 图片方向与 EXIF

JPG 可能包含 EXIF Orientation。

要求：

打开图片后显示方向必须和 Windows 图片查看器一致。

如果 SkiaSharp 默认读取不能自动处理 EXIF Orientation：

- 在 `ImageFileService` 中统一处理。
- 加载后把像素实际旋转到正确方向。

保存时不需要保留原 EXIF Orientation。

第一版重点保证：

> 用户看到的方向正确。

---

# 27. 性能

目标：

普通图片：

```text
1920 × 1080
3840 × 2160
```

应做到：

- 打开不卡死。
- 拖选区域实时。
- 裁切基本瞬间完成。
- Undo / Redo 可接受。

---

## 27.1 超大图

至少测试：

```text
10000 × 10000
```

要求：

- 不因为简单拖动持续复制原图。
- 只有确认裁切时才创建新 Bitmap。
- 选区预览只画 Overlay。
- 不在 PointerMoved 中反复重新编码图片。

---

# 28. 内存管理

SkiaSharp 对象注意释放：

- `SKBitmap`
- `SKImage`
- `SKData`
- `SKCanvas`

历史栈删除旧状态时：

- Dispose 对应 Bitmap。

打开新图片：

- Dispose 旧文档。
- Dispose Undo / Redo 历史。

不要依赖 GC 最终回收所有图像资源。

---

# 29. 边界情况

## 29.1 选区太小

如果删除宽度 / 高度小于：

```text
2 px
```

取消操作。

避免误触。

---

## 29.2 删除整个图片

禁止：

```text
cutWidth >= imageWidth
```

或：

```text
cutHeight >= imageHeight
```

提示：

```text
无法删除整张图片
```

---

## 29.3 从图片外开始拖动

只有鼠标按下位置在图片有效区域内：

才允许开始中间裁切。

---

## 29.4 拖出图片范围

允许拖出显示区域。

最终坐标 Clamp：

```text
0 ~ width
0 ~ height
```

---

## 29.5 反向拖动

必须支持：

```text
下 -> 上
右 -> 左
```

统一使用：

```text
min(start, end)
max(start, end)
```

---

# 30. 连续裁切

这是重要使用场景。

用户确认一次裁切后：

- 中间裁切工具继续激活。
- 图片重新居中或保持当前合理视图。
- 用户可以马上拖第二段。

例如：

```text
原图
↓
删除广告 1
↓
删除空白 2
↓
删除多余内容 3
↓
保存
```

Undo 必须可以逐步撤回：

```text
撤回第 3 次
撤回第 2 次
撤回第 1 次
```

---

# 31. 保存后的历史

保存后：

- 不清空 Undo。
- 用户仍可继续撤销。

但：

```text
IsDirty
```

状态必须正确。

推荐给每个历史状态维护一个递增版本号：

```text
DocumentVersion
SavedVersion
```

当：

```text
DocumentVersion == SavedVersion
```

则：

```text
IsDirty = false
```

否则：

```text
IsDirty = true
```

如果实现版本号反而让代码复杂，可以采用同等清晰方案。

重点是：

> Undo 回到已保存状态时，标题中的 `*` 应自动消失。

---

# 32. 主数据模型

建议：

```csharp
ImageDocument
{
    string? FilePath;
    SKBitmap CurrentBitmap;
    bool IsDirty;
}
```

历史：

```csharp
HistoryManager
{
    Stack<ImageState> Undo;
    Stack<ImageState> Redo;
}
```

裁切：

```csharp
CutOutOperation
{
    SKBitmap Execute(
        SKBitmap source,
        int start,
        int end,
        CutDirection direction
    );
}
```

方向：

```csharp
enum CutDirection
{
    Horizontal,
    Vertical
}
```

UI 不要直接实现底层像素复制算法。

---

# 33. ImageCanvas 职责

`ImageCanvas` 只负责：

- 绘制图片。
- 缩放显示。
- 平移。
- 鼠标坐标转换。
- 绘制裁切 Overlay。
- 抛出用户完成选区事件。

例如：

```csharp
CutSelectionCompleted(
    CutDirection direction,
    int startPixel,
    int endPixel
)
```

真正修改 Bitmap：

由 Core 层处理。

这样最容易调试。

---

# 34. CutOutOperation 职责

只负责：

```text
输入：
source bitmap
start
end
direction

输出：
new bitmap
```

不负责：

- UI
- 提示框
- 文件保存
- Undo
- 鼠标坐标

保持为纯图像操作。

---

# 35. 文件打开职责

`ImageFileService`：

```text
Load
Save
SaveAs
```

负责：

- 文件格式。
- EXIF orientation。
- 编码。
- 文件异常。

不要散落在 MainWindow。

---

# 36. 原文件安全

保存时采用：

```text
写入临时文件
→ 成功
→ 替换目标文件
```

目的：

避免写文件过程中异常导致原图损坏。

这里仅做基本原子保存。

不要增加：

- hash
- checksum
- 多重备份
- 复杂事务系统

---

# 37. UI 细节

## 37.1 中间裁切按钮状态

未激活：

```text
中间裁切
```

激活：

- 有明显背景高亮。
- 鼠标指针变化。

---

## 37.2 鼠标指针

横向裁切识别后：

可显示上下调整型光标。

纵向裁切识别后：

可显示左右调整型光标。

如果实现麻烦：

第一版统一十字光标即可。

不要为光标延迟核心开发。

---

# 38. 窗口尺寸

默认：

```text
1200 × 800
```

最小：

```text
800 × 600
```

记住用户上次窗口尺寸和位置可以作为后续功能。

第一版不是必须。

---

# 39. 主题

第一版：

- 跟随系统浅色 / 深色主题，若 Avalonia FluentTheme 可直接支持。
- 不开发自定义主题系统。

---

# 40. 测试

不要大量堆 UI 自动化测试。

重点测试真正容易出错的核心逻辑。

---

## 40.1 CutOutOperation 单元测试

必须覆盖：

### 横向

原：

```text
100 × 100
```

删除：

```text
Y 40 ~ 60
```

结果：

```text
100 × 80
```

并验证：

- 上半部分像素正确。
- 下半部分从 Y=40 开始。
- 中间区域不存在。

---

### 纵向

原：

```text
100 × 100
```

删除：

```text
X 25 ~ 40
```

结果：

```text
85 × 100
```

---

### 反向坐标

```text
60 -> 40
```

结果必须等价于：

```text
40 -> 60
```

---

### 无效区域

```text
start == end
```

不修改图片。

---

### 删除整图

必须拒绝。

---

# 41. 坐标转换测试

至少覆盖：

原图：

```text
3840 × 2160
```

显示：

```text
960 × 540
```

scale：

```text
0.25
```

屏幕：

```text
Y 200 ~ 300
```

转换后：

```text
Y 800 ~ 1200
```

---

# 42. Undo / Redo 测试

流程：

```text
打开图片
→ Cut A
→ Cut B
→ Undo
→ Undo
→ Redo
→ Redo
```

每一步：

- 图片尺寸正确。
- 像素内容正确。
- `CanUndo`
- `CanRedo`
- `IsDirty`

都正确。

---

# 43. 手工验收

Codex 完成后必须实际运行程序并检查。

---

## Case 1

打开：

```text
3840 × 2160 JPG
```

横向删除：

```text
300 px
```

预期：

```text
3840 × 1860
```

---

## Case 2

纵向删除：

```text
400 px
```

预期：

```text
3440 × 当前高度
```

---

## Case 3

连续删除 3 次。

预期：

- 每次自动合拢。
- 无空白。
- 无缩放。
- 无拉伸。

---

## Case 4

连续：

```text
Ctrl+Z
Ctrl+Z
Ctrl+Y
```

结果正确。

---

## Case 5

图片以 25% 显示。

拖动选区。

实际删除位置和屏幕看到的位置一致。

---

## Case 6

PNG 带透明区域。

裁切后：

- Alpha 保留。

---

## Case 7

覆盖保存 JPG。

重新打开。

尺寸和画面正确。

---

# 44. 视觉验收

最重要：

裁切连接处不能出现：

- 1 px 空白线。
- 黑线。
- 透明线。
- 重复行。
- 少一行。
- 多一行。

这是核心验收标准。

---

# 45. 第一版完成标准

以下全部满足才算完成：

- [ ] Windows 能正常启动。
- [ ] 可以打开 JPG / PNG / WebP / BMP。
- [ ] 可以拖拽文件进入。
- [ ] 点击“中间裁切”后进入裁切状态。
- [ ] 启动时不会默认进入中间裁切状态。
- [ ] 可以横向删除中间区域。
- [ ] 可以纵向删除中间区域。
- [ ] 选区自动扩展到完整宽度 / 高度。
- [ ] 拖动时有半透明预览。
- [ ] 显示删除 px 和结果尺寸。
- [ ] 松开后等待确认。
- [ ] Enter 确认。
- [ ] Esc 取消。
- [ ] 裁切后自动合拢。
- [ ] 不拉伸、不重采样。
- [ ] 支持连续裁切。
- [ ] Ctrl+Z 撤销。
- [ ] Ctrl+Y 重做。
- [ ] 支持保存。
- [ ] 支持另存为。
- [ ] 修改未保存时有 `*`。
- [ ] 关闭未保存文件时提示。
- [ ] 缩放后裁切坐标仍正确。
- [ ] PNG Alpha 正确。
- [ ] 连接处不存在 1 px seam。
- [ ] Release 构建成功。
- [ ] Windows 10 实机可运行。

---

# 46. Codex 开发要求

Codex 在开始编码前：

1. 阅读本 SPEC。
2. 阅读目标仓库现有文件。
3. 如果已有 `AGENTS.md`，先遵守。
4. 检查当前工作树。
5. 不覆盖用户已有修改。
6. 先查看 ShareX 相关 CutOut 实现。
7. 再独立设计本项目最小实现。

---

# 47. 实施顺序

严格按下面顺序做，不要同时铺开大量功能。

### Phase 1

完成：

- 项目可启动。
- 打开图片。
- 显示图片。
- 适合窗口。

### Phase 2

完成：

- CutOutOperation。
- 横切。
- 竖切。
- 核心单元测试。

### Phase 3

完成：

- 鼠标拖选。
- 横 / 竖自动判断。
- Overlay。
- 坐标转换。

### Phase 4

完成：

- 确认 / 取消。
- 连续裁切。

### Phase 5

完成：

- Undo / Redo。

### Phase 6

完成：

- 保存。
- 另存为。
- 未保存提醒。

### Phase 7

完成：

- Zoom。
- Pan。
- 快捷键。

### Phase 8

完成：

- 手工验收。
- Release 构建。
- 修正实际发现的问题。

不要提前开发 Phase 8 之外的功能。

---

# 48. Codex 输出要求

完成后必须报告：

```text
1. 实现了什么
2. 修改 / 新增了哪些文件
3. 核心裁切算法放在哪里
4. Undo / Redo 怎么实现
5. ShareX 只参考了哪些设计
6. 是否直接复制过 ShareX 代码
7. 执行了哪些测试
8. 测试结果
9. Release 构建结果
10. EXE / 发布目录在哪里
11. 仍存在的已知问题
```

不要只回复：

```text
Done
```

---

# 49. 代码原则

- 优先解决当前明确需求。
- 保持实现小而清楚。
- 不做无关重构。
- 不引入大型依赖解决简单问题。
- 不建立复杂抽象层。
- 不为了未来假设场景提前设计。
- 核心图像操作必须容易单元测试。
- UI 和图像算法分离。
- 出错时给用户明确反馈。
- 任何破坏性文件操作必须避免直接损坏原文件。

---

# 50. 最终产品体验

用户实际操作应接近：

```text
打开图片
↓
点击“中间裁切”
↓
在图片中拖出不要的一段
↓
看到遮罩和删除尺寸
↓
Enter / 点击确认
↓
中间区域消失
↓
两边自动合拢
↓
继续裁下一段
↓
Ctrl + S
```

核心目标不是“功能多”。

核心目标是：

> 比 PhotoDemon 简单得多，
> 达到 Picsew 这类工具的低学习成本，
> 同时在 Windows 上把“删除图片中间一段并自动合拢”做到稳定、快速、准确。
